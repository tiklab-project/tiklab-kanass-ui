import React from "react";
import { UserGroup } from 'tiklab-user-ui';

const ProjectUserGroup = (props) => {
    return (
        <UserGroup bgroup = "kanass" isBase = {false} />
    )
}
export default ProjectUserGroup;