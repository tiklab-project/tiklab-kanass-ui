import React from "react";
import {FlowChart} from 'tiklab-flow-ui';

const FlowDetailDesign = (props) => {
    return (
        <FlowChart isBase = {false} />
    )
}
export default FlowDetailDesign;