import React from "react";
import {PreliminaryList} from 'tiklab-form-ui';

const ProjectProjectDirectory = (props) => {
    return (
        <PreliminaryList isBase = {true} />
    )
}
export default ProjectProjectDirectory;