import React from "react";
import {PreliminaryTypeList} from 'tiklab-form-ui';

const ProjectProjectDirectory = (props) => {
    return (
        <PreliminaryTypeList isBase = {true} />
    )
}
export default ProjectProjectDirectory;